import type { PluginClientContext } from "@getpaseo/plugin/client";

export default function contribute(_client: PluginClientContext) {
  // Header button + workspace panel land in T2.
  return () => {};
}
